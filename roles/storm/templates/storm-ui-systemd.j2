[Unit]
Description=Apache Storm UI
Requires=network.target
After=network.target

[Service]
User={{ bd_user }}
Group={{ bd_group }}
ExecStart={{ storm_install_dir }}/bin/storm --config {{ storm_install_dir }}/conf/storm.yaml ui
Restart=on-failure

[Install]
WantedBy=multi-user.target